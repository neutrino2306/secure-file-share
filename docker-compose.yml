services:
  api:
    build: .
    ports:
      - "${HOST_PORT:-8000}:8000"
    env_file:
      - path: .env
        required: false
    environment:
      DATA_DIR: /data
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
